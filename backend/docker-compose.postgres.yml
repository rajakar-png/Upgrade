services:
  postgres:
    image: postgres:16
    container_name: astranodes-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: astranodes
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d astranodes"]
      interval: 10s
      timeout: 5s
      retries: 8

volumes:
  postgres_data:
