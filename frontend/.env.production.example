# ─────────────────────────────────────────────────────────────
#  AstraNodes — Frontend Production Environment Variables
#  Rename to .env.production before running: npm run build
#  deploy.sh writes this file automatically.
# ─────────────────────────────────────────────────────────────

# Full URL to your API — must NOT have a trailing slash
VITE_API_URL=https://astranodes.cloud/api

# Full URL for Socket.io connection (same domain, no /path)
VITE_SOCKET_URL=https://astranodes.cloud
